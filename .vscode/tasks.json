{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Debug Build",
			"type": "shell",
			"command": "ls -la ${workspaceRoot}; rm -rvf debug; mkdir debug; cd debug; cmake -DCMAKE_BUILD_TYPE=Debug -Og ..; make"
		}
	]
}